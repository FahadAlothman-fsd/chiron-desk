# @chiron/sandbox-engine

Execution isolation and git workspace module (planned).

## What This Package Contains

- `src/index.ts` - scaffold export

## Current State

- Standalone package is scaffold-only
- Git/worktree behavior is currently distributed and not centralized

## Intended Scope

- Worktree lifecycle per execution
- Branch naming and isolation policies
- Safe cleanup after execution completion/interruption

## Naming Note

- Package naming has been aligned to `sandbox-engine` to reflect broader isolation scope beyond git-only concerns.
