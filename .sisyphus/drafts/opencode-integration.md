# Draft: OpenCode SDK Integration Plan

## Requirements (confirmed)
- Create a detailed plan for integrating OpenCode SDK into Chiron agent-runtime and PM orchestration.
- Plan must include: architecture choice (single server vs per-project), session lifecycle, tool registration (static tools), worktree strategy, event streaming, data model impacts, verification steps.
- Provide decision matrix and recommended default.

## Technical Decisions
- Pending: architecture choice, session lifecycle model, tool registration approach, worktree strategy, event streaming mechanism, data model changes.

## Research Findings
- Pending: codebase patterns and external docs.

## Open Questions
- Desired scope boundaries (in/out) for integration depth.
- Preferred testing/verification strategy (TDD, tests-after, manual-only) based on existing test infra.
- Any constraints on deployment topology or infra (single server vs per-project).

## Scope Boundaries
- INCLUDE: planning details listed in requirements.
- EXCLUDE: implementation/execution steps beyond planning.
