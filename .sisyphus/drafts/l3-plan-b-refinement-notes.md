# Plan B Refinement Notes

## Running Log of Plan-B-Relevant Findings

> Each entry must include:
> - Task: the Plan A task number/title
> - Files: concrete file paths touched or discovered
> - Reality: what the implementation proved true
> - Plan B Impact: what should change, narrow, expand, or reorder in Plan B
> - Evidence: test name or command proving the reality

### 2026-04-16 — Task 1: Lock Plan A action/branch/gate contracts
- Task: T1 — Lock Plan A contracts, semantics, and explicit Plan B deferrals
- Files: `packages/contracts/src/methodology/workflow.ts`, `packages/contracts/src/runtime/executions.ts`, `packages/contracts/src/runtime/conditions.ts`, `packages/contracts/src/tests/l3-plan-a-action-branch-contracts.test.ts`
- Reality: Plan A can lock Action to propagation-only whole-step authoring and keep branch save-selection semantics explicit without introducing a richer nested `work_unit_draft_spec_fact` runtime payload. The minimal overlap only needs narrow `exists | equals` runtime condition operators plus opaque `comparisonJson`/`subFieldKey` escape hatches.
- Plan B Impact: Keep the nested `work_unit_draft_spec_fact` runtime/value contract, canonical value normalization, shared typed fact-instance model, and broad operator convergence grouped in Plan B; they are still additive scope beyond the Plan A contract floor.
- Evidence: `bunx vitest run packages/contracts/src/tests/l3-plan-a-action-branch-contracts.test.ts`

### 2026-04-17 — Task 3: Add action runtime schema and repositories
- Task: T3 — Add Action-step runtime schema/repositories for lazy creation and retry-in-place semantics
- Files: `packages/db/src/schema/runtime.ts`, `packages/db/src/runtime-repositories/action-step-runtime-repository.ts`, `packages/workflow-engine/src/repositories/action-step-runtime-repository.ts`, `packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts`, `packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts`
- Reality: Action runtime persistence can stay thin by using a root `action_step_executions` row keyed to generic `step_executions`, action rows keyed by design-time `actionDefinitionId`, and item rows keyed by design-time `itemDefinitionId`. Lazy creation works cleanly by idempotently creating the root/action/item rows on first execution, and retry semantics can update the same action/item rows in place without any history tables.
- Plan B Impact: Keep runtime rows normalized around design-time IDs plus compact result payloads; defer any richer attempt history, action-definition snapshots, or propagation-item-specific recovery/state fan-out unless a later service layer proves they are necessary.
- Evidence: `bunx vitest run packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts -t "action runtime"`; `bunx vitest run packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts -t "action runtime"`


### 2026-04-17 — Task 4: Add whole-step action authoring services and router
- Task: T4 — Implement Action whole-step authoring service/router surfaces with propagation-only validation
- Files: `packages/methodology-engine/src/services/action-step-definition-service.ts`, `packages/methodology-engine/src/tests/action-step-definition-services.test.ts`, `packages/methodology-engine/src/index.ts`, `packages/api/src/routers/methodology.ts`, `packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts`
- Reality: Plan A whole-step action authoring can stay thin: the service only needs draft-version gating, step-key uniqueness, workflow-context ownership checks, propagation-only `actionKind` enforcement, allowed-kind enforcement, and pass-through preservation of authored `actionId` / `itemId` order so downstream repo delta reconciliation can keep stable nested IDs without service-side normalization.
- Plan B Impact: Keep Plan B focused on richer repo/runtime behavior, not extra authoring-layer mutation semantics. Stable nested IDs are easiest to preserve when the authoring service does not rewrite sort/order structures or synthesize IDs before persistence.
- Evidence: `bunx vitest run packages/methodology-engine/src/tests/action-step-definition-services.test.ts`; `bunx vitest run packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts -t "action methodology"`

### 2026-04-17 — Task 2: Add action design-time schema and repositories
- Task: T2 — Add Action-step methodology tables and whole-step delta repositories
- Files: `packages/db/src/schema/methodology.ts`, `packages/db/src/methodology-repository.ts`, `packages/db/src/repositories/action-step-definition-repository.ts`, `packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts`, `packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts`
- Reality: Stable-ID whole-step reconciliation works cleanly when the relational layer treats `actionId` / `itemId` as the durable nested identity and lets repository validation own higher-level uniqueness for `actionKey`, `contextFactDefinitionId`, and `itemKey`. Reusing strict DB uniqueness on mutable authored keys makes in-place edit/swap flows fragile during delta updates, while stable-ID reconciliation plus repository-level validation preserves edit-in-place semantics.
- Plan B Impact: Keep Plan B delta authoring centered on durable authored IDs, and avoid adding extra mutable-key uniqueness constraints that would force delete/recreate churn or temporary shadow values during whole-step graph edits.
- Evidence: `bunx vitest run packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts -t "action methodology"`; `bunx vitest run packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts -t "action design-time"`

### 2026-04-17 — Task 5: Add branch runtime persistence and localized evaluator helpers
- Task: T5 — Add Branch runtime state rows plus narrow route-evaluation helpers for Plan A
- Files: `packages/db/src/schema/runtime.ts`, `packages/db/src/runtime-repositories/branch-step-runtime-repository.ts`, `packages/workflow-engine/src/repositories/branch-step-runtime-repository.ts`, `packages/workflow-engine/src/services/branch-route-evaluator.ts`, `packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts`, `packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts`, `packages/workflow-engine/src/tests/runtime/branch-runtime-services.test.ts`
- Reality: Branch runtime can stay compact with a single mutable root row per `stepExecutionId` plus a replace-free route cache keyed by authored `routeId`. The evaluator only needs workflow-context facts, `exists | equals`, and branch-local decoding rules for plain/external values, workflow refs, artifact refs, and draft-spec `fact:<id>` / `artifact:<id>` selectors to produce valid conditional-route suggestions for Plan A.
- Plan B Impact: Keep the evaluator localized until branch and transition gates actually need a shared fact/value model. When Plan B broadens operator support or canonicalizes draft-spec paths, migrate from branch-local selectors to a truly shared decoder layer instead of widening this helper opportunistically.
- Evidence: `bunx vitest run packages/db/src/tests/schema/l3-plan-a-action-branch-schema.test.ts -t "branch runtime"`; `bunx vitest run packages/db/src/tests/repository/l3-plan-a-action-branch-repositories.test.ts -t "branch runtime"`; `bunx vitest run packages/workflow-engine/src/tests/runtime/branch-runtime-services.test.ts -t "evaluator"`

### 2026-04-17 — Task 5: Add action runtime orchestration/detail services
- Task: T5 — Implement Action runtime orchestration, manual completion gating, and canonical runtime detail assembly
- Files: `packages/workflow-engine/src/services/action-step-runtime-service.ts`, `packages/workflow-engine/src/services/action-step-detail-service.ts`, `packages/workflow-engine/src/services/step-execution-detail-service.ts`, `packages/workflow-engine/src/services/step-execution-transaction-service.ts`, `packages/workflow-engine/src/layers/live.ts`, `packages/workflow-engine/src/index.ts`, `packages/workflow-engine/src/tests/runtime/action-step-runtime-services.test.ts`
- Reality: Plan A runtime can keep Action execution thin as long as orchestration is definition-driven: lazy root/action/item rows keyed by authored IDs, sequential skip-ahead blocking derived from prior action success, parallel selected-action runs, retry-in-place for `needs_attention`, and completion gating based only on "at least one succeeded action" plus "no running actions". Definition-backed external facts can already support future outward rebinding by normalizing runtime context values into `{ factInstanceId, value }` envelopes at execution time without adding history tables or extra step-state rows.
- Plan B Impact: Keep richer propagation semantics (real external/artifact commit services, recovery workflows, attempt history, broader validation hardening) explicitly separate from the thin orchestration layer. If Plan B needs concrete target commits, preserve the current authored-ID runtime row model and add commit adapters under it instead of redesigning the action/action-item lifecycle surface.
- Evidence: `bunx vitest run packages/workflow-engine/src/tests/runtime/action-step-runtime-services.test.ts`

### 2026-04-17 — Task 7: Branch lifecycle/save-selection/progression lock semantics
- Task: T7 — Implement branch activation state creation, explicit save-selection, completion-time revalidation, and persisted-selection-driven progression
- Files: `packages/workflow-engine/src/services/step-execution-lifecycle-service.ts`, `packages/workflow-engine/src/services/step-progression-service.ts`, `packages/workflow-engine/src/services/step-execution-transaction-service.ts`, `packages/workflow-engine/src/services/workflow-execution-step-command-service.ts`, `packages/workflow-engine/src/services/workflow-execution-detail-service.ts`, `packages/workflow-engine/src/services/workflow-execution-command-service.ts`, `packages/workflow-engine/src/tests/runtime/branch-runtime-services.test.ts`
- Reality: Branch runtime only remains deterministic when completion/progression both re-evaluate current valid routes and require persisted `selectedTargetStepId` to still be valid. Suggestion/default information can remain advisory for UI, but command paths must reject missing/invalid persisted selection and never silently remap to the current first suggestion.
- Plan B Impact: If Plan B introduces richer branch route/value semantics, keep a single shared "revalidation authority" used by step completion, next-step activation, and workflow terminal checks to avoid drift between detail, completion, and progression interpretations.
- Evidence: `bunx vitest run packages/workflow-engine/src/tests/runtime/branch-runtime-services.test.ts -t "save and complete" --reporter=verbose`; `bunx vitest run packages/workflow-engine/src/tests/runtime/branch-runtime-services.test.ts -t "progression" --reporter=verbose`

### 2026-04-17 — Task 8: Extend runtime gate/operator overlap for Plan A
- Task: T8 — Extend runtime gate and transition-gate condition overlap only for Action/Branch Plan A behavior
- Files: `packages/workflow-engine/src/services/runtime-gate-service.ts`, `packages/workflow-engine/src/services/transition-gate-conditions.ts`, `packages/workflow-engine/src/tests/runtime/runtime-gate-alignment.test.ts`
- Reality: The safe Plan A overlap is still narrow, but branch/runtime drift disappears once transition-gate decoding preserves `equals`, `subFieldKey`, `comparisonJson`, and root/group `all|any` modes, and runtime-gate fact evaluation reuses the same value-shape semantics for `exists`/`equals` (including empty-array falsehood and draft-spec `fact:` / `artifact:` / `instance:` selectors).
- Plan B Impact: Promote the duplicated branch/gate value extraction into a shared decoder only when Plan B broadens operator families or fact normalization. Until then, keep overlap limited to the current selectors/operators so transition gates stay aligned without forcing full fact unification early.
- Evidence: `bunx vitest run packages/workflow-engine/src/tests/runtime/runtime-gate-alignment.test.ts`

### 2026-04-17 — Task 9: Add action runtime router procedures and SSE stream
- Task: T9 — Expose Action runtime start/run/retry/complete commands and ordered Action-only SSE
- Files: `packages/api/src/routers/project-runtime.ts`, `packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts`, `packages/contracts/src/sse/envelope.ts`, `packages/workflow-engine/src/services/runtime/action-step-event-stream-service.ts`, `packages/workflow-engine/src/layers/live.ts`, `packages/workflow-engine/src/index.ts`, `apps/server/src/index.ts`, `apps/server/src/tests/sse/action-step-events.test.ts`, `apps/server/vitest.config.ts`
- Reality: Plan A Action streaming can stay narrow and deterministic without introducing generic workflow events or persisted event tables. A step-scoped incremental stream is sufficient when it emits only bootstrap, item/action status deltas, completion-eligibility changes, error, and done, and derives ordering from the authored action/item sort order plus the existing runtime detail surface.
- Plan B Impact: If later work wants resumable/replayable history or broader workflow streaming, add a separate event-log/replay design instead of widening the Plan A Action stream. The current polling/diff seam keeps Plan A isolated to Action execution semantics while preserving space for a future shared event backbone.
- Evidence: `bunx vitest run packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts -t "action runtime"`; `bunx vitest run apps/server/src/tests/sse/action-step-events.test.ts`; `bun run build`

### 2026-04-17 — Task 10: Branch detail API/read model and step execution UI
- Task: T10 — Expose branch runtime detail payload, save-selection mutation, and shared step-execution UI
- Files: `packages/workflow-engine/src/services/step-execution-detail-service.ts`, `packages/api/src/routers/project-runtime.ts`, `packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts`, `apps/web/src/routes/projects.$projectId.step-executions.$stepExecutionId.tsx`, `apps/web/src/tests/routes/runtime-branch-step-detail.test.tsx`
- Reality: Branch runtime stays consistent only when the detail query itself carries persisted-selection validity, advisory suggestion/default metadata, and completion-block reasons from the server. The shared UI can safely handle single/no/multi-match states by rendering those server fields directly and using save-selection as the only mutation that can unlock completion; suggestion-only states must remain incomplete until explicitly saved.
- Plan B Impact: If Plan B adds richer branch diagnostics or route labels, extend the branch detail read model instead of letting the client recompute validity from raw conditions. Keep `selectedTargetStepId` as the sole completion authority and treat any richer route analytics as read-only augmentation around that persisted-selection contract.
- Evidence: `bunx vitest run packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts -t "branch runtime"`; `bunx vitest run apps/web/src/tests/routes/runtime-branch-step-detail.test.tsx`

### 2026-04-17 — Task 12: Seed setup workflow and transition gate fixtures
- Task: T12 — Seed the setup workflow and transition gate fixtures for end-to-end coverage
- Files: `packages/scripts/src/seed/methodology/setup/setup-invoke-phase-1-fixture.ts`, `packages/scripts/src/seed/methodology/setup/setup-bmad-mapping.ts`, `packages/scripts/src/seed/methodology/tables/index.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-action-steps.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-action-step-actions.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-action-step-action-items.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-branch-steps.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-branch-routes.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-branch-route-groups.seed.ts`, `packages/scripts/src/seed/methodology/tables/runtime-methodology-workflow-branch-route-conditions.seed.ts`, `packages/scripts/src/tests/seeding/setup-invoke-phase-1-fixture.test.ts`, `packages/scripts/src/tests/seeding/methodology-seed-integrity.test.ts`
- Reality: Plan A seed realism works best when the setup invoke fixture extends the existing topology instead of inventing a parallel workflow. The locked action slice can stay narrow with three propagation-only authored actions anchored to existing setup context facts, while branch coverage only needs explicit route rows plus lightweight plain/workflow-reference synthesis outputs. Transition gate seeds can move to the post-refactor non-empty shape without pulling in broader operator families: `exists | equals`, root/group `all|any`, and current fact/artifact identifiers are enough.
- Plan B Impact: Keep Plan B away from richer seeded payload assumptions. If Plan B revisits seed expressiveness, expand around a shared seed-builder or shared fact/value normalization layer rather than embedding draft-spec-specific or multi-fact propagation semantics directly into the Plan A fixture shape.
- Evidence: `bunx vitest run packages/scripts/src/tests/seeding/setup-invoke-phase-1-fixture.test.ts`; `bunx vitest run packages/scripts/src/tests/seeding/methodology-seed-integrity.test.ts`

### 2026-04-17 — Task 11: Action editor/runtime web surfaces
- Task: T11 — Implement Action-step editor/runtime web surfaces
- Files: `apps/web/src/routes/methodologies.$methodologyId.versions.$versionId.work-units.$workUnitKey.workflow-editor.$workflowDefinitionId.tsx`, `apps/web/src/routes/projects.$projectId.step-executions.$stepExecutionId.tsx`, `apps/web/src/features/projects/execution-detail-visuals.tsx`, `apps/web/src/tests/routes/action-step-editor.test.tsx`, `apps/web/src/tests/routes/action-step-execution.test.tsx`
- Reality: The Action editor only stays stable when the route hydrates whole-step Action payloads from per-step definition queries before handing them to the shared shell, and the runtime page stays trustworthy only when run/retry/completion affordances render the server-authored locked reasons directly instead of inferring availability from client-side row state.
- Plan B Impact: If Plan B adds richer Action row drilldowns or recovery workflows, keep the current server-authoritative whole-step hydration and runtime-action gating contract intact; extend with richer read-model fields instead of moving action availability or completion eligibility logic into the client.
- Evidence: `bunx vitest run apps/web/src/tests/routes/action-step-editor.test.tsx --reporter=verbose`; `bunx vitest run apps/web/src/tests/routes/action-step-execution.test.tsx --reporter=verbose`

### 2026-04-17 — Task 13: Add integration, backward-compat guards, and Plan A regression coverage
- Task: T13 — Add integration, backward-compat guards, and Plan A regression coverage
- Files: `packages/workflow-engine/src/tests/runtime/runtime-gate-alignment.test.ts`, `packages/workflow-engine/src/tests/runtime/runtime-invoke-completion.test.ts`
- Reality: Plan A stays safer when regression coverage locks two compatibility seams explicitly: runtime gates/branch overlap must treat minimal invoke-produced draft-spec reference payloads as valid for `exists` checks without requiring richer nested `facts/artifacts/instance` shapes, and transition-gate decoding must continue collapsing unknown operators back to the narrow `exists | equals` Plan A set. Invoke completion outputs should also remain ID-reference payloads only, with no richer nested fact/artifact payload leakage.
- Plan B Impact: Keep richer nested invoke payloads and broader operator convergence clearly deferred. If Plan B expands draft-spec payload richness or operator families, introduce those as explicit contract/service changes rather than letting tests or runtime helpers silently assume them.
- Evidence: `bunx vitest run packages/workflow-engine/src/tests/runtime/runtime-gate-alignment.test.ts`; `bunx vitest run packages/workflow-engine/src/tests/runtime/runtime-invoke-completion.test.ts`

### 2026-04-17T00:24Z — Task 13: Strict-scope verification classification only
- Task: T13 — Record strict Plan A verification status without widening implementation scope
- Files: `.sisyphus/notepads/l3-plan-a-action-branch-gates/issues.md`, `.sisyphus/drafts/l3-plan-b-refinement-notes.md`, failing repo-root paths observed in latest verification: `apps/server/src/mcp/route.ts`, `packages/agent-runtime/src/opencode-harness-service.ts`, `packages/db/src/runtime-repositories/agent-step-execution-harness-binding-repository.ts`, `packages/sandbox-engine/src/services/sandbox-git-service.ts`, `packages/workflow-engine/src/services/runtime/agent-step-*`, `apps/desktop/src/tests/packaging/server-build-config.test.ts`
- Reality: The latest verification still splits cleanly between scoped and unscoped work. Strict Plan A remains Action + Branch + narrow gate overlap only, and the repo-root failures now sit in deferred harness/MCP/sandbox and agent-runtime hardening surfaces outside that boundary. `bun run check-types` failed and `bun run test` failed at repo root, while `bun run build` passed. Targeted Plan A suites remained green across Action runtime/router/SSE, Branch runtime/detail, and the T13 regression guards.
- Plan B Impact: Keep Plan B explicit about the next hardening tranche instead of treating repo-root failures as evidence that Plan A scope should expand again. The deferred follow-up set should stay grouped around harness bindings, MCP route hardening, sandbox git services, agent-step runtime services, and desktop packaging expectations for built server artifacts, while Plan A remains locked to the narrower Action/Branch/gate contract and regression surface.
- Evidence: `bun run check-types` (failed); `bun run test` (failed); `bun run build` (passed); `bunx vitest run packages/workflow-engine/src/tests/runtime/runtime-gate-alignment.test.ts` (passed); `bunx vitest run packages/workflow-engine/src/tests/runtime/runtime-invoke-completion.test.ts` (passed); `bunx vitest run packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts -t "action runtime"` (passed); `bunx vitest run packages/api/src/tests/routers/action-branch-plan-a-routers.test.ts -t "branch runtime"` (passed); `bunx vitest run apps/server/src/tests/sse/action-step-events.test.ts` (passed); `bunx vitest run apps/web/src/tests/routes/action-step-editor.test.tsx --reporter=verbose` (passed); `bunx vitest run apps/web/src/tests/routes/action-step-execution.test.tsx --reporter=verbose` (passed); `bunx vitest run apps/web/src/tests/routes/runtime-branch-step-detail.test.tsx` (passed)
