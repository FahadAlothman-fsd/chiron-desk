- 2026-04-09: Froze Agent-step contracts under `packages/contracts/src/{agent-step,mcp,sse}/**` with a v1 boundary that encodes explicit session start, bootstrap-only continuation, one `agent_step_session_events` SSE stream, three MCP tools only (`read_step_snapshot`, `read_context_value`, `write_context_value`), and `applied_only` persistence semantics for writes.
- 2026-04-09: Encoded the Agent-step runtime state machine as the exact six-state set `not_started | starting_session | active_streaming | active_idle | disconnected_or_error | completed` plus an explicit allowed-transition contract so later runtime services can reject forbidden paths without inventing new states.
- 2026-04-09: Normalized error contracts to Chiron-facing harness/OpenCode + MCP families (`HarnessExecutionError`, `OpenCodeExecutionError`, validation/requirement/state/stream errors) and intentionally excluded speculative provider-specific families and any `request_context_access` v1 surface.
- 2026-04-09: Agent-step design-time persistence now uses four tables in `packages/db/src/schema/methodology.ts`: the child row stores objective/instructions, harness/model, locked runtime-policy fields, and completion requirements JSON; explicit reads persist only declared grants; write-card requirements persist required workflow context-fact definition ids (never write-item ids), with repository-level guards for duplicate reads, duplicate write cards, and self-dependencies.
- 2026-04-09: Methodology-engine Agent-step design-time services now reuse `WorkflowEditorDefinitionService` as the base read seam, derive inferred reads from write targets only, derive read-mode previews from `(context fact kind, source)` pairs, and normalize write-item order to deterministic `100`-based display slots before persistence.
- 2026-04-09: `WorkflowAuthoringTransactionService.applyMutation(...)` now exposes `createAgentStep`, `updateAgentStep`, and `deleteAgentStep` slots, ordered so context-fact creates/updates happen before Agent-step writes while Agent-step deletes happen before context-fact deletes, preserving same-call binding and teardown behavior.
- 2026-04-09: The design-time Agent-step UI works best as a local AI Elements-style composition rather than a registry install: a copied `ModelSelector` built from the AI Elements registry can live under `apps/web/src/features/workflow-editor/agent-step-tabs/` and reuse the existing `command` + `dialog` primitives without introducing extra global component churn.
- 2026-04-09: In the workflow editor, context-fact rebinding has to touch both step families: Form steps need field binding remaps, while Agent steps need explicit reads, write targets, write requirements, and completion requirements rewritten when a context-fact definition id changes.
- 2026-04-09: Minimal OpenCode design-time discovery only needs `client.app.agents()` plus `client.config.providers()`; that is enough to normalize the authoring-time harness payload into agent/provider/model metadata without creating sessions or implementing runtime chat behavior.
- 2026-04-09: `packages/agent-runtime` now freezes a v1 `HarnessService` seam around Chiron DTOs only: session start takes `{ stepExecutionId, agent?, model?, objective, instructionsMarkdown }`, runtime commands return normalized `HarnessSessionStarted | HarnessMessageAccepted | HarnessTimelinePage`, errors stay inside `HarnessDiscoveryError | HarnessExecutionError | OpenCodeExecutionError`, and fake-harness tests can drive runtime work by replaying one `agent_step_session_events` SSE stream plus timeline pages from in-memory session state.
- 2026-04-09: Agent-step runtime persistence now splits cleanly across `agent_step_execution_state` (Chiron-owned state machine + `bootstrapAppliedAt`), `agent_step_execution_harness_binding` (one unique binding row per `step_execution_id` with harness/session/server/model data), and `agent_step_execution_applied_writes` (append-only successful applied context-fact updates with fact kind, instance order, and applied value JSON).
- 2026-04-09: `packages/sandbox-engine/src/services/git-query-service.ts` now provides a reusable `Context.Tag` git truth seam with typed `NotAGitRepoError` and `InvalidPathError`, repo-relative path normalization, commit/blob resolution, commit file listing, and artifact-oriented `hasFileAtCommit` / `matchesBlobHash` checks so later Agent runtime services can stay free of direct git command logic.
- 2026-04-09: `packages/sandbox-engine` needed its own `vitest.config.ts` with `src/tests/**/*.test.ts` inclusion, otherwise the required `bunx vitest run packages/sandbox-engine/src/tests/git-query-service.test.ts` command could not discover the new sandbox-engine seam tests from the workspace.
- 2026-04-09: Agent-step runtime orchestration in `packages/workflow-engine/src/services/runtime/**` works cleanly when the domain layer derives immutable methodology-version context on demand (`project pin -> work-unit type -> workflow editor + agent-step definition`), keeps harness binding/state separate from step completion status, and computes write exposure from requirement satisfaction only so fake-harness MCP tests can validate idempotent session start, single-stream enforcement, and applied-write-only persistence without leaking OpenCode-specific types.
- 2026-04-09: `packages/agent-runtime/src/opencode-harness-service.ts` now owns the full OpenCode adapter boundary: managed servers are started per `stepExecutionId` with `config.mcp.chiron.url = /mcp?stepExecutionId=...`, bootstrap context is injected once with `noReply`, raw OpenCode message/tool/event shapes are normalized into Chiron timeline + SSE DTOs, session event history is replayable for late subscribers, and failed session/bootstrap startup paths explicitly close the spawned server before returning typed `OpenCodeExecutionError`/`HarnessDiscoveryError` results.
- 2026-04-09: Managed OpenCode server metadata now has to be treated as durable binding state, not just in-memory adapter state: `agent_step_execution_harness_binding` stores both `serverInstanceId` and `serverBaseUrl`, `HarnessSessionStarted` carries them back from agent-runtime, and runtime bind/update paths should overwrite both on rebinding so respawned servers refresh DB truth without changing the existing idempotent start/health-check behavior.
- 2026-04-09: The runtime API surface now delegates Agent-step detail/timeline/session mutations and the single `agent_step_session_events` stream directly to workflow-engine services from `packages/api/src/routers/project-runtime.ts`; the stream bridge stays thin by converting the Effect `Stream` to an `AsyncIterable` with `Stream.toAsyncIterable`, so the API layer exposes one oRPC/SSE-compatible live feed without re-owning runtime state logic.
- 2026-04-09: `apps/server/src/mcp/route.ts` should stay execution-scoped and transport-thin: bind MCP requests from `?stepExecutionId=...`, register only the three v1 tools (`read_step_snapshot`, `read_context_value`, `write_context_value`), and delegate each tool call to `AgentStepMcpService` while returning structured JSON content rather than leaking workflow-engine internals or OpenCode-specific types.
- 2026-04-09: The runtime Agent-step detail page works cleanly as a two-query surface: keep `getRuntimeStepExecutionDetail` for the shared shell, layer `getAgentStepExecutionDetail` for the agent body, and drive the side panel from `getRuntimeWorkflowExecutionDetail.workflowContextFacts` so MCP `write_context_value` completions only need query invalidation on the workflow-detail + agent-detail keys rather than a second live stream.
- 2026-04-09: A thin local AI Elements-style `PromptInput` wrapper is enough for Chiron’s runtime composer when attachments are out of scope; pair it with the copied AI Elements `ModelSelector`, let turn-selection mutations persist before or during a session, and treat `active_streaming` as frontend-disabled even if the backend composer contract stays broadly enabled for the live session state.
- 2026-04-09: Reusing `use-sse.ts` for Agent-step runtime required making it generic over event names/data and enabling `withCredentials`; a dedicated `/sse/agent-step-session-events?projectId=...&stepExecutionId=...` bridge in `apps/server/src/index.ts` can forward the workflow-engine stream as named SSE events without adding a second runtime feed.
