- Runtime work-unit identity now lives with the runtime row itself: `project_work_units` carries immutable `work_unit_key`, monotonic `instance_number`, and optional `display_name` metadata gated to `many_per_project` definitions.
- Project and work-unit runtime fact CRUD can preserve lineage with logical delete semantics by inserting `deleted` tombstones and superseding prior active rows instead of hard-deleting family-owned facts.
- Workflow-context runtime CRUD needs both a mutable current-state projection (`workflow_execution_context_facts` with `instance_id`) and an append-only audit trail (`workflow_execution_context_fact_records`) so `remove` vs `delete` remain distinguishable.
- The workspace had widespread canonical-fact and invoke-source drift (`bound_external_fact`/`definition_backed_external_fact`, `workflow_reference_fact`, `artifact_reference_fact`, `fixed_set`, `context_fact_backed`) that had to be normalized across db, API, runtime, MCP, and harness layers before typecheck could stabilize again.
- Shared runtime manual CRUD can stay target-agnostic by resolving scope-specific methodology metadata first, then running one canonical value-normalization pass before delegating to the target repository's create/update/remove/delete semantics.
- Workflow-context family policy is easiest to keep explicit when `bound_fact`, `workflow_ref_fact`, `artifact_snapshot_fact`, and `work_unit_draft_spec_fact` each get their own runtime decoder/validator rather than relying on one loose `unknown` path.
- Artifact snapshot writes are safer when runtime normalization stores one canonical `{ slotDefinitionId, files[] }` group per slot and converts folder-bound relative inputs into repo-relative file paths before persistence.
- Slot-level path rules can stay contract-free at runtime by treating `rulesJson` as advisory metadata and normalizing a small shared subset (`pathPattern`/`pathRegex` + optional folder-path context fact binding) inside a dedicated service.
- The `record_deleted_file` vs `remove_from_slot` distinction fits naturally as write-time operations: deleted files remain in the grouped payload with `status: "deleted"`, while removals drop the file from the slot state without implying any repository deletion.
- Runtime work-unit reads can stay contract-stable while still surfacing identity by enriching service outputs with `workUnitKey`, `instanceNumber`, and `displayName`, plus using the immutable key as the display instance id.
- Progressive disclosure for `work_unit_draft_spec_fact` reads works cleanly by storing only runtime ids in context values, then resolving `{key,type,display}` summaries by default and revealing fact/artifact id collections only when `query` mode includes `expand=facts,artifacts`.
- Agent-step runtime test support must provide `ArtifactSnapshotServiceLive` anywhere `AgentStepContextWriteServiceLive` or merged runtime layers are constructed, otherwise unrelated read-path tests can fail during layer assembly with a missing `ArtifactSnapshotService`.
- Project and work-unit manual runtime CRUD now route through shared `RuntimeManualFactCrudService` cleanly when the API surface exposes only `create/update/delete` and lets repository-backed logical delete create tombstones instead of direct router mutations.
- Project fact detail rows are typed by the shared runtime fact contract as `instanceId`, so UI CRUD forms need to map that generic identifier back into project-specific mutation inputs rather than assuming a `projectFactInstanceId` field on detail responses.
- Project runtime now exposes workflow-context manual CRUD as dedicated router procedures (`create/update/remove/delete`) backed by `RuntimeManualFactCrudService`, which keeps workflow-context-only semantics out of project/work-unit mutation flows.
- Progressive-disclosure reads stay contract-stable by treating `queryParam` as a validated mini-protocol: only `instanceId`, `expand`, and positive-integer `limit` are accepted, and `expand` is reserved for `work_unit_draft_spec_fact` reads.
- The workflow execution detail page can remain the canonical manual CRUD home without changing the detail contract by layering workflow-context CRUD mutations on top of the existing grouped read model.
- A tiny shared bound-fact envelope normalizer lets runtime readers accept both legacy `{ factInstanceId, value }` rows and canonical `{ instanceId, value }` rows while keeping all new prefill/action/detail writes on the canonical shape.
- Form/detail and invoke consumers need two distinct bound-fact views: reference controls should surface canonical envelopes, while propagation/equality consumers should unwrap to the underlying value before evaluating or materializing downstream facts.
- Invoke work-unit runs can keep `work_unit_draft_spec_fact` template-only by freezing per-target draft payloads (stable `draftKey`, resolved fact values, grouped `artifactSlots[].files[]`) into invoke target state at start time, while leaving created work-unit/fact/artifact ids isolated in invoke materialization mappings.
- Task 11 audit: all project/work-unit/workflow-context manual runtime mutation router procedures in `packages/api/src/routers/project-runtime.ts` now delegate to `RuntimeManualFactCrudService.apply`, so approved manual CRUD entry points share one canonical normalization/validation path.
- Task 11 audit: `RuntimeManualFactCrudService` enforces canonical workflow-context families (`bound_fact`, `workflow_ref_fact`, `artifact_snapshot_fact`, `work_unit_draft_spec_fact`) before persistence, keeps project/work-unit logical delete semantics centralized, and preserves workflow-context `remove` vs `delete` through the repository split.
- Task 11 audit: workflow-context mediation still flows through `StepContextMutationService.replaceContextFacts` and `InvokePropagationService`, which replace context facts using `sourceStepExecutionId` and emit frozen draft-spec payloads rather than direct project/work-unit mutations.
- Task 11 regression result: targeted no-bypass tests passed (`runtime-manual-fact-crud-service`, `runtime-workflow-context-fact-crud`, `project-runtime-mutations`, `agent-step-write-exposure`, `invoke-draft-spec-template-state`), but the required Plan A regression pack, full `bun run test`, `agent-step-mcp-services`, and `bun run build` still fail on contract/test drift around renamed canonical fact families (`bound_fact`, `artifact_snapshot_fact`) and invoke source modes (`fixed`/`fact_backed`).
- Final Verification F4 (2026-04-19): scope fidelity held. Targeted review found no methodology-version thinning, no broad runtime-UI redesign outside the three approved detail surfaces, no direct step writes to project/work-unit facts, no raw JSON-first dialogs for special families, no repo-file deletion behavior, no friendly slot-key persistence at canonical runtime boundaries, and no created runtime ids stored in draft-spec templates.
- `@chiron/contracts` stale-contract failures cleared by updating tests to the Plan B canonical vocabulary (`bound_fact`, `workflow_ref_fact`, `artifact_snapshot_fact`, `fixed`, `fact_backed`) and by narrowing MCP response unions with discriminant/property checks before reading `output` fields.
- Desktop packaging coverage is more stable when it asserts the desktop bundle script contract (`bundle-desktop-runtime.mjs` copying `dist/index.mjs` to `server.mjs` and vendoring runtime packages) instead of assuming pre-bundled `dist/node_modules` artifacts already exist during plain test runs.
- The runtime router boundary guard now needs an overhead check (`serviceResolutionCount - handlerCount`) rather than a 1:1 cap, because the expanded Plan B CRUD surface intentionally resolves a small number of extra services while still forbidding repository imports.
- Methodology-engine Plan B test updates split into two patterns: canonical fixtures/read previews must move to `bound_fact` / `workflow_ref_fact` / `artifact_snapshot_fact` and `fixed` / `fact_backed`, while compatibility tests that intentionally exercise legacy seed outputs should keep old labels if the underlying seed fixture has not yet been migrated.
- Large seed-fixture imports in `packages/scripts` are stable once their Vitest timeout is raised to 15s; the previous 5s limit was too aggressive for dynamic import plus row-materialization work.
- The version-service test repo must mirror the production `WorkflowSnapshot` contract by storing `transitionWorkflowBindings` in scoped form (`Record<workUnitTypeKey, Record<transitionKey, workflowKeys[]>>`); keeping the old flat shape causes publish tests to fail early with `PUBLISH_REQUIRED_CONTRACT_INCOMPLETE` before repository-level publish diagnostics are reached.
- Deterministic publish/versioning assertions are still meaningful when they lock diagnostic ordering and codes, but repeated `validateDraftVersion` calls should only assert that timestamps are valid ISO values, not byte-for-byte identical wall-clock strings.
- `packages/workflow-engine` runtime tests now expect Plan B canonical shapes end-to-end: invoke tests use `fixed` / `fact_backed`, workflow refs use `workflow_ref_fact`, artifact writes/readbacks use grouped `{ slotDefinitionId, files[] }`, and work-unit invoke propagation stores draft-spec payloads as `{ workUnitDefinitionId, draftKey, factValues, artifactSlots }` rather than legacy runtime id bundles.
- Runtime test layers must provide the same transitive services the live runtime composition expects; missing `BranchStepRuntimeRepository`, `StepProgressionService`, `ActionStepDetailService`, or `ActionStepRuntimeService` in tests can surface as unrelated runtime failures long before the scenario under test is exercised.
- Bound-fact runtime expectations have diverged by consumer: form/detail and action propagation tests should now assert canonical envelopes with `instanceId`, while primitive-bound form submissions may surface the unwrapped value when the field resolves as a primitive control rather than a reference selector.

- 2026-04-19 16:55: Small web test typing regressions are safest to fix at the mock boundary, not by weakening assertions—giving `vi.fn` explicit mutation input types preserves the existing `mock.calls[n][0]` payload checks and clears tuple-index errors. The companion invoke/branch design-time suites stay aligned with Plan B once their fixtures use canonical `sourceMode` values (`fixed`, `fact_backed`) end-to-end. Verified with `bun run --filter web check-types`, `bun --filter @chiron/api test -- src/tests/routers/l3-design-time-invoke-branch-router.test.ts`, and `bun --filter @chiron/db test -- src/tests/repository/l3-design-time-invoke-branch-repositories.test.ts`.

- 2026-04-19 17:01: For the current workflow-editor web blockers, the fastest trustworthy confirmation path is still targeted route-suite reruns first, then full `bun --filter web test`; in this case all three previously reported suites already matched the canonical UI contracts and passed without further selector, wait-strategy, or payload changes.
