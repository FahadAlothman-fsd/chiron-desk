- `bun run check-types` is currently failing in pre-existing `packages/workflow-engine/src/services/**` files with canonical workflow-context fact kind and agent-step contract mismatches unrelated to the runtime repository changes in this task.
- `bun run check-types` now clears the workflow-engine changes for this task, but the workspace still fails in pre-existing `apps/web/src/routes/**` and related test files with unrelated canonical fact/UI contract mismatches outside the artifact snapshot runtime slice.
- `bun run check-types` now reports no remaining plain TypeScript shape errors in the touched runtime/db paths, but the workspace still exits non-zero because of remaining Effect language-service diagnostics/messages in other subsystems (for example `sandbox-git-service`, `runtime-manual-fact-crud-service`, and general Effect guidance diagnostics). Evidence is in `.sisyphus/evidence/task-2-types.log`.
- `bun run check-types` remains blocked by broad pre-existing web-route/test typing failures outside task 6 (workflow-editor, step-execution, desktop API mocks, and other runtime UI routes), even after clearing the new project/work-unit CRUD router and route errors. Fresh evidence is in `.sisyphus/evidence/task-6-types.log`.
- Final Verification F1 rerun on 2026-04-19 still fails: `bun run check-types` exits non-zero in `apps/web` with widespread canonical fact/invoke contract drift, and standalone `bun run test` fails in `@chiron/contracts` on stale Plan A / slice tests still expecting legacy kinds/source modes such as `bound_external_fact`, `workflow_reference_fact`, `artifact_reference_fact`, `fixed_set`, and `context_fact_backed`. Evidence: `.sisyphus/evidence/f1-plan-compliance.log`.
- After fixing the originally requested desktop/API/agent-step blockers, `bun run test` still fails in unrelated methodology-version publish/versioning assertions (`packages/methodology-engine/src/tests/versioning/version-service.test.ts`) that now observe `PUBLISH_REQUIRED_CONTRACT_INCOMPLETE` and timestamp drift, plus downstream workspace EPIPE noise once that package aborts. Evidence: `.sisyphus/evidence/fix-remaining-tests-full.log`.
- Workflow-editor Plan B follow-up: `workflow-editor-shell.tsx`, the workflow-editor route, and local workflow-editor helpers/types were tightened around canonical invoke modes (`fixed` / `fact_backed`), context-fact slot semantics, and exact-optional object shaping. Primary shell/route files are LSP-clean after the local fixes.
- `apps/web` `bun run check-types` still fails after this wave because additional workflow-editor cluster files (`dialogs.tsx`, `agent-step-dialog.tsx`, runtime-fact dialogs/tests) and unrelated pre-existing web/runtime files still have remaining exact-optional and branch/operator typing drift. The fresh run also confirms broader non-task blockers remain in `use-sse`, methodology facts routes, transition execution routes, tests, and workflow-engine Effect diagnostics.
- Follow-up pass tightened more web-only compile paths without touching backend packages: replaced `use-sse` JSON parsing with plain typed `try/catch`, widened methodology fact validation typing to include `allowed-values`, reduced optional-draft failures in runtime fact dialogs, narrowed several workflow-editor dialog/operator/source-kind paths, and broadened workflow-editor test fixture shapes so context-fact mock payloads can carry canonical Plan B fields.
- LSP remains clean on the newly touched web files from this follow-up, but I did not get a fresh post-fix `check-types` proof after the second apps/web status run because the session policy caps status checks. Last known apps/web blockers before the final edits were still centered in workflow-editor dialog/agent-step/runtime-fact/test typing.
- Narrow pass on 2026-04-19 removed the listed `runtime-fact-dialogs`, tuple-index, methodology-facts mock, and transition-execution route errors. After the second allowed `apps/web` status check, two web TS blockers still remained: one workflow-editor branch operator literal narrowing in `dialogs.tsx`, and one active/completed detail union mismatch in `runtime-branch-step-detail.test.tsx`. Workspace-level messages from `packages/workflow-engine` also still appear, but they are Effect guidance messages rather than this pass’s target web errors.
- Final web cleanup: the last two `apps/web` TypeScript errors were fixed; `apps/web` `bun run check-types` is now clean apart from non-failing Effect guidance messages emitted from `packages/workflow-engine`.
- `@chiron/scripts` seed-artifact timeout hardening: added explicit 15s suite timeouts to `l3-slice-1-demo-fixture.test.ts`, `methodology-seed-integration.test.ts`, and `methodology-seed-integrity.test.ts` because their first dynamic methodology/fixture imports are materially heavier under broader `bun run test` load, while assertions and seed-validation intent remain unchanged. `bun --filter @chiron/scripts test` now passes.
- Runtime validation follow-up on 2026-04-19: the requested failing web tests now pass after tightening stale route-context mocks for `mutationOptions` / `queryOptions`, adding `useNavigate` coverage in shell tests, fixing workflow-context CRUD call assertions to read React Query variable args instead of mutation context args, normalizing unavailable runtime reference handling for canonical `bound_external_fact` / `workflow_reference_fact` / `artifact_reference_fact` aliases in step-execution detail, and updating invoke-branch fixtures from legacy `fixed_set` to canonical `fixed`.
- A fresh `bun --filter web test` rerun still fails in additional unrelated web suites outside the originally listed runtime-validation targets (for example project dashboard route tests, action-step editor tests, methodology-version shell route tests, runtime project/work-unit/workflow route tests, and workflow-editor form-slice tests with older expectation labels/payload shapes). The specifically requested runtime shell/form-detail/workflow-context/action-step tests are green, and `bun --filter web build` succeeds.
- Canonical-only web regression cleanup completed on 2026-04-19: `bun --filter web test` now passes after updating stale test mocks for button exports and ORPC query/mutation contracts, migrating dashboard/workflow-editor/runtime assertions to canonical labels and payload fields (`Bound Fact Definition Id`, `workUnitDefinitionId`, `fixed`, runtime CRUD copy), and aligning methodology shell-route expectations with the newer work-unit/workflow/artifact-slot mutation payload shapes.

- 2026-04-19: Fixed backend Plan A/Plan B compatibility regressions across api/db tests. Added legacy-to-canonical normalization for action/agent/invoke authoring payloads and workflow-context fact hydration, normalized scoped transition bindings for older snapshot shapes, updated public snapshot consumers to flatten bindings, and refreshed backend test fixtures/expectations for canonical Plan B names plus current schema columns. Residual blockers: none in api/db packages; full workspace `bun run test` still includes non-backend suites outside this task scope.

- 2026-04-19: Removed active legacy context-fact compatibility from db/api/methodology-engine action and agent flows, switched schema checks and repo writes/reads to canonical kinds only, and migrated touched api/db/web tests/fixtures to canonical `bound_fact`, `workflow_ref_fact`, and `artifact_snapshot_fact` fields. Scripts package passes and targeted `action-step-editor` web suite passes. Newly surfaced blockers from full `bun --filter web test`: project dashboard/create-project shell route assertions, runtime workflow execution detail test mocks missing workflow-context mutation stubs, and workflow-editor form-slice expectations still using older labels/shape assumptions (`External Fact Definition Id`, legacy draft-spec expectation fields).

- 2026-04-19: Restored missing `WorkflowSnapshot` type import in `packages/methodology-engine/src/services/methodology-version-service.ts`. The original missing-symbol errors are resolved; `bun run check-types` now fails on separate invoke typing issues in `packages/api/src/routers/methodology.ts` and `packages/db/src/methodology-repository.ts`, not in methodology-version-service.

- 2026-04-19: Fixed the remaining API/DB invoke typing regressions by restoring literal Zod `sourceMode` unions in `packages/api/src/routers/methodology.ts` and adding explicit discriminant-based branch extraction before invoke payload property access in `packages/db/src/methodology-repository.ts`. `bun run check-types` now advances past those files and fails only on separate pre-existing web/runtime test type issues outside this task scope.

- 2026-04-19 16:55: Cleared the targeted Plan B regression blockers without widening scope. Fixed the unused `runtimeWorkflowExecutionShellQueryKey` import in `apps/web/src/tests/routes/runtime-execution-route-shells.test.tsx`, typed the workflow-context CRUD mutation mocks in `apps/web/src/tests/runtime/workflow-context-fact-crud-section.test.tsx` so `mock.calls[n][0]` assertions stay tuple-safe, and migrated stale invoke test fixtures in the targeted API/DB suites from `sourceMode: "fixed_set"` to canonical `sourceMode: "fixed"`. Passed commands: `bun run --filter web check-types` (exit 0; only non-failing Effect guidance messages), `bun --filter @chiron/api test -- src/tests/routers/l3-design-time-invoke-branch-router.test.ts`, and `bun --filter @chiron/db test -- src/tests/repository/l3-design-time-invoke-branch-repositories.test.ts`.

- 2026-04-19 17:01: Re-ran the three reported web blockers exactly as listed plus full `bun --filter web test`. None of the reported failures reproduced: `action-step-editor`, `workflow-editor-form-slice.integration`, `workflow-editor-invoke-branch.integration`, and the full web suite all passed cleanly. No additional web source/test edits were required in this pass.
