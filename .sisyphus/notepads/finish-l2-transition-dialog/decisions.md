- 2026-03-29: Wrapped `createProjectWorkUnit` failures with operation-specific `RepositoryError` in `startTransitionExecution` to make failure origin explicit.
- 2026-03-29: Added regression test that proves `futureCandidate` path creates a work unit with `currentStateId = transition.fromStateId` and uses the created ID for transition start.
- 2026-03-29: Updated runtime start-transition API surface (contracts + ORPC zod input + web launch decision) to a single discriminated `workUnit` payload, preventing callers from sending both or neither context forms.
