- 2026-03-29: `startTransitionExecution` now supports `futureCandidate` by resolving transition context with `workUnitTypeId` (without requiring an existing `projectWorkUnitId`) and creating a new project work unit from `transition.fromStateId`.
- 2026-03-29: Keeping transition-gate resolution centralized avoids drift; `resolveTransitionGateContext` can now derive `workUnitTypeId` from either existing work unit lookup or direct `futureCandidate` input.
- 2026-03-29: Transition start/switch mutations now surface errors to users via sonner toasts, reusing `toErrorMessage` so failures are visible without altering success paths.
- 2026-03-29: `StartTransitionExecutionInput` now uses explicit `workUnit.mode` discrimination (`existing` vs `new`) to remove ambiguous optional-field combinations and make backend branch logic exhaustive.
