- Slice-1 contracts now centralize reusable invariants in `packages/contracts/src/shared/invariants.ts` (`DescriptionJson`, `SetupTags`) and reuse those across workflow/runtime DTOs.
- Form step create/update parity is enforced by a single shared `FormStepPayload`; command schemas only differ by identity envelope fields.
- Context-fact contract taxonomy is frozen with 7 explicit kinds (`plain_value`, `external_binding`, `workflow_reference`, `work_unit_reference`, `artifact_reference`, `draft_spec`, `draft_spec_field`) to prevent drift.
- Non-Form step models remain explicitly deferred/default in slice-1 (`mode: "deferred"` + `defaultMessage`), while runtime write commands remain Form-only.
- `setup_tags` should remain a fact-level invariant contract (`Record<string,string>`), not a built-in `FormStepPayload` field.
- Slice-1 DB schema now adds dedicated design-time Form tables (`methodology_workflow_form_steps`, `methodology_workflow_form_fields`) while keeping generic `methodology_workflow_steps`/`edges` as topology anchors; repositories keep Form step IDs aligned with workflow step IDs for edge compatibility.
- Typed workflow context-fact persistence is split into a definition table + 7 specialized tables keyed by `fact_kind`, matching the frozen contract kinds exactly (`plain_value`, `external_binding`, `workflow_reference`, `work_unit_reference`, `artifact_reference`, `draft_spec`, `draft_spec_field`).
- Runtime slice-1 step core now has explicit tables for activation/submission/context writes (`step_executions`, `form_step_execution_state`, `workflow_execution_context_facts`), with repository seams centered on create-step, upsert-form-state, and context-fact-write operations.
- Baseline canonical seed expectations remain L1/L2 (`methodology_workflow_steps` and `methodology_workflow_edges` still zero), and the new deterministic demo fixture is isolated in `seed/methodology/setup/slice-1-demo-fixture.ts` with WU.SETUP `Form -> Form` (2 steps, 1 edge, all 7 context-fact examples).
- Runtime step-core now exposes reusable workflow-engine services (`StepExecutionLifecycleService`, `StepProgressionService`, `StepContextQueryService`, `StepContextMutationService`, `StepExecutionTransactionService`) backed by a dedicated runtime repository seam that owns step lifecycle rows, form snapshots, context-fact writes, and workflow topology reads for deterministic first-step activation and progression.
- Slice-1 authoring procedures are now explicitly namespaced under `version.workUnit.workflow.*` with dedicated form/edge/context-fact operations (`getEditorDefinition`, `updateWorkflowMetadata`, Form CRUD, edge CRUD, contextFact CRUD) rather than generic step-save endpoints.
- One-outgoing-edge enforcement is implemented in `WorkflowTopologyMutationService` at the service boundary by checking current workflow edges before create/update mutations and failing deterministically on duplicate source-step exits.
- Form-step create/update paths intentionally share the same `FormStepPayload` object contract in services and router schemas; only identity envelope fields differ (`afterStepKey` for create, `stepId` for update).
- Generic edge conditions remain explicitly deferred in slice-1: topology mutations persist edge descriptions (`{ markdown }`) while branch condition behavior is forced to `null`.
- Workflow editor route now resolves deterministic identity from explicit path param (`$workflowDefinitionId`) and falls back to workflow key only when legacy rows do not expose a canonical definition id yet.
- The slice-1 web shell is now split into reusable workflow-editor feature modules (`step-types-grid`, `step-list-inspector`, `workflow-canvas`, `dialogs`, `workflow-editor-shell`) while preserving the locked dark left-rail structure and exact section labels (`STEP TYPES`, `STEP LIST & INSPECTOR`).
- UI-level one-outgoing-edge enforcement is now centralized in the shell connect handler before mutation dispatch; duplicate source-step connections are rejected locally so invalid edge commands do not get sent.
- Slice-1 runtime command flow is now split into dedicated workflow-engine services: `WorkflowExecutionStepCommandService` (activation + command orchestration), `FormStepExecutionService` (draft/save + submit semantics), and `StepExecutionDetailService` (tab-ready detail model with Submission & Progression, Writes, Context Fact Semantics).
- First-step activation is explicit and idempotent at the workflow-execution command boundary: it validates project ownership, reuses an existing first step when present, and only creates the first step on explicit `activateFirstWorkflowStepExecution` calls.
- Form submit now preserves immutable submitted snapshots, records workflow-context writes from form values, maps `project.<factDefinitionId>` keys into authoritative project fact writes, and keeps progression lineage visible through step detail (`nextStepExecutionId`, write tabs, and semantics notes).
- Runtime web slice now exposes explicit first-step activation from workflow execution detail when no step rows exist; once active, the same surface collapses into compact runtime summary text instead of a deferred placeholder.
- New `/projects/$projectId/step-executions/$stepExecutionId` route uses three tab surfaces (Submission & Progression, Writes, Context Fact Semantics) and always carries semantic copy clarifying immutable submit snapshots, progression lineage, workflow-context writes, and authoritative project-fact writes.
- Project creation now collects optional `projectRootPath` with normalization preview and absolute-path validation in UI before submit; payload includes normalized `projectRootPath` when present.
- Project fact list/detail now call out manual-authoring scope explicitly as project-fact-only and add instance status indicators plus direct add-value entry cues.
- Slice-1 verification now locks all five later step types (`agent`, `action`, `invoke`, `branch`, `display`) to deferred/default read models at architecture-test level, so any future non-Form authoring or runtime command path addition fails deterministically.
- Runtime/service seam coverage now proves progression stays reusable for later slices by activating a non-Form next step (`agent`) through the shared lifecycle/progression path without introducing non-Form runtime command procedures.
- Web integration coverage now explicitly guards that locked later-step tiles remain deferred UI only and that runtime manual authoring hooks are absent from non-project-fact routes.
- Demo fixture verification now checks the dedicated WU.SETUP slice stays isolated from the canonical baseline while preserving deterministic external binding/reference seeds (`projectRootPath`, workflow ref, work-unit ref, artifact ref, draft-spec field).
- `packages/methodology-engine/src/layers/live.ts` must not `Layer.mergeAll` `WorkflowAuthoringTransactionServiceLive` alongside its dependencies; wiring it with `Layer.provide(WorkflowAuthoringTransactionServiceLive, MethodologyEngineL1CoreServicesLive)` removes runtime `Service not found: WorkflowMetadataService` defects in router-level execution.
- `WorkflowMetadataService` has been fully consolidated into `WorkflowService`; transaction/router code paths now source metadata updates from `WorkflowService.updateWorkflowMetadata`, and L1 live layer wiring should include `WorkflowServiceLive` (not a separate metadata service layer).
- `MethodologyVersionService` constructs a local `WorkflowService` façade via `WorkflowService.of(...)`; when the `WorkflowService` contract gains a method, that façade must be updated in lockstep or type-checking fails in downstream package builds.
- API dependency leakage fix pattern: keep `createProjectRouter` dual-layer aware (`baseServiceLayer` for lightweight project/methodology projection endpoints, optional `runtimeServiceLayer` for runtime endpoints) and pass the split explicitly from `createAppRouter`; this prevents `/rpc/project/listProjects` from constructing runtime-only layers that require `StepExecutionRepository`.
- Workflow editor step-type tiles now resolve icon imagery directly from public assets using deterministic code-based URLs (`/visuals/workflow-editor/step-types/asset-{iconCode}.svg`), matching the existing status badge asset pattern and keeping tile headers image-first rather than code-text placeholders.
- For dark-shell SVG assets authored in pure black, applying Tailwind filter utilities (`invert` + brightness/contrast tuning) on `<img>` is a low-risk contrast fix that avoids editing source SVG files and preserves per-tile enabled/locked visual hierarchy with opacity.
- Workflow canvas readability improves when replacing fixed `h-[42rem]` with viewport-aware height (`h-[calc(100dvh-12rem)]`) plus `min-h-[42rem]`, allowing the editor to use available vertical space on tall screens while maintaining a reliable minimum interaction area.
- `FormStepDialog` now follows the work-unit authoring tab pattern with explicit local tab state (`contract` default + `fields` + `context-facts`), while preserving slice-1 behavior by gating save on non-empty step key and persisting only contract-authored payload fields (`key`, optional `label`, optional `descriptionJson`, existing `fields/contextFacts`).
- Workflow context-fact list runtime fix: `MethodologyRepository` now exposes `listWorkflowContextFactsByDefinitionId({ versionId, workflowDefinitionId })`, and DB layer implementation verifies workflow existence first, returns `[]` for workflows with no context facts, and maps persisted context-fact rows into `WorkflowContextFactDto`; router error mapping now surfaces repository operation + cause in 500 messages for actionable diagnostics.
- Canonical ID flow fix: workflow list payload now carries `workflowDefinitionId` (DB primary id), web navigation to `/workflow-editor/$workflowDefinitionId` is guarded to require that canonical id, and workflow editor route no longer resolves/accepts `workflow.key` as an ID fallback for context-fact/editor RPCs.
- Desktop-safe folder picking for create-project should stay behind a narrow IPC channel (`desktop:select-project-root-directory`) exposed as a single preload bridge method (`selectProjectRootDirectory`), with web route behavior keeping manual input as baseline and treating desktop browse as an optional enhancer that writes into existing `projectRootPath` normalization/validation flow.
- Preload bridge resilience pattern: export a small `exposeDesktopBridge` helper that (a) uses `contextBridge.exposeInMainWorld` when `contextIsolation=true`, (b) logs explicit errors if bridge exposure API is unavailable/fails, and (c) falls back to direct `window.desktop` assignment only when `contextIsolation=false`; renderer UX should distinguish "no desktop bridge" from "partial/outdated desktop bridge" to provide actionable restart guidance.
- Renderer bridge compatibility for Electron method renames should resolve a single callable in priority order (new API first, then legacy optional method) so capability checks and Browse behavior stay correct across mixed preload versions without changing web-only fallback behavior.
- Electron main launched from source TypeScript must still resolve preload to a compiled JS artifact (e.g. `dist/desktop/preload.js`) and dev startup should build first, otherwise Electron attempts to execute raw `.ts` preload and bridge initialization never occurs.
- Desktop preload runtime fix: keep Electron `main` on the compiled `dist/desktop/main.js`, but build preload separately as `dist/desktop/preload.cjs` via `bun build --format=cjs` and a test-only `tsconfig.build.json`; this avoids sandboxed preload ESM parse failures (`Cannot use import statement outside a module`) while preserving the narrow `selectProjectRootDirectory` bridge surface.
- 2026-04-04T05:06:56+03:00: `WorkflowEngineRuntimeStepServicesLive` must not `Layer.mergeAll` sibling step layers that depend on each other (e.g., `WorkflowExecutionStepCommandServiceLive` requiring `StepProgressionService`); compose with explicit `Layer.provideMerge` dependency stages (core → lifecycle/tx/form/command/detail) so runtime DI resolves deterministically.
- 2026-04-05: Workflow canvas node cards stay visually aligned with the dark editor shell when `workflow-canvas.tsx` overrides the heavy-frame `--frame-bg` token to `var(--background)` directly; removing the local canvas header also requires the root section to own the full viewport-aware height so ReactFlow can fill the remaining space without introducing a white/light band above the graph.
