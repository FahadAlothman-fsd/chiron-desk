{
  "name": "ai-service",
  "private": true,
  "version": "0.1.0",
  "description": "FastAPI AI service for Chiron BMAD workflows",
  "scripts": {
    "dev": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload",
    "build": "echo 'No build step required for Python service'",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "lint": "uv run ruff check app/",
    "format": "uv run black app/",
    "type-check": "uv run mypy app/",
    "test": "uv run pytest"
  },
  "dependencies": {},
  "devDependencies": {}
}